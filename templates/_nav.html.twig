<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
	<a class="navbar-brand" href="{{path('classroom_index')}}">Student Management</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>
	<div class="collapse navbar-collapse" id="navbarNav">
		<ul class="navbar-nav">
			<li class="nav-item">
				<a class="nav-link" href="{{path('classroom_index')}}">Classroom</a>
			</li>

			<li class="nav-item">
				<a class="nav-link" href="{{path('course_index')}}">Course</a>
			</li>

			<li class="nav-item">
				<a class="nav-link" href="{{path('teacher_index')}}">Teacher</a>
			</li>

			<li class="nav-item">
				<a class="nav-link" href="{{path('student_index')}}">Student</a>
			</li>
		</ul>
	</div>

	<div class="form-inline my-2 my-lg-0">
		<p class="form-control mr-sm-2 mb-0">Welcome: {{ app.user.username }}!</p>
		<a class="btn btn-outline-success my-2 my-sm-0" href="{{path('app_logout')}}">Logout</a>
	</div>
</nav>

